---
import { path } from '../../lib/path';

interface Props {
  avatarSrc?: string;
}

const { avatarSrc = path('/avatar.png') } = Astro.props;
---

<div class="arch-scene" aria-hidden="true">
  <img src={avatarSrc} alt="" width="1000" height="1000" />
  <slot />
</div>
