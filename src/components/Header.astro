---
import { site } from '../data/site';
import { path } from '../lib/path';
---

<header>
  <a href={path('/')}>{site.name}</a>
  <nav aria-label="Main">
    <a href={path('/writing/')}>Writing</a>
  </nav>
</header>
